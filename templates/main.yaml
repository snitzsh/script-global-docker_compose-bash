{{- $components := .Values.components -}}
{{- $docker := .Values.docker -}}
{{- $posgres_name := "postgres" -}}

version: "3.8"
services:
  {{- include "docker-compose.functions.services" . }}
{{- if $docker.volumes }}
{{- include "volumes" . }}
{{- end }}
{{- if $docker.networks }}
{{- include "networks" . }}
{{- end }}
